<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OurApp.WinUI.GamePage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OurApp.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="using:OurApp.Core.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:GameViewModel}"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Padding="40" Spacing="25" HorizontalAlignment="Center" VerticalAlignment="Center">

            <StackPanel Visibility="{Binding IsStartVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding WelcomeMessage, Mode=OneWay}" 
                FontSize="28" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}" 
                TextAlignment="Center" Margin="0,0,0,20"/>
                <Button Content="Să începem!" Command="{Binding StartGameCommand}" HorizontalAlignment="Center" Padding="20,10"/>
            </StackPanel>

            <StackPanel Visibility="{Binding IsChoiceActive, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding CurrentQuestion, Mode=OneWay}" 
                FontSize="18" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="500" Margin="0,0,0,20"/>

                <ItemsControl ItemsSource="{Binding CurrentChoices, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <Button Content="{Binding}" Margin="0,5,0,5" HorizontalAlignment="Stretch"
                         Command="{Binding ElementName=PageRoot, Path=ViewModel.SelectChoiceCommand}"
                         CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <StackPanel Visibility="{Binding IsReactionActive, Mode=OneWay, Converter={StaticResource BoolToVis}}" Spacing="20">
                <TextBlock Text="Reacția colegului:" FontSize="14" Foreground="Gray" TextAlignment="Center"/>
                <TextBlock Text="{Binding Feedback, Mode=OneWay}" 
                FontSize="20" FontStyle="Italic" Foreground="LightGreen" 
                TextAlignment="Center" TextWrapping="Wrap" MaxWidth="450"/>
                <Button Content="Continuă" Command="{Binding GoToNextStepCommand}" HorizontalAlignment="Center" Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>

            <StackPanel Visibility="{Binding IsConclusionVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Concluzie" FontSize="28" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="{Binding Feedback, Mode=OneWay}" FontSize="18" TextAlignment="Center" MaxWidth="500"/>
                <Button Content="Ieșire" Command="{Binding ExitCommand}" HorizontalAlignment="Center" Margin="0,30,0,0"/>
            </StackPanel>

        </StackPanel>
        <Frame x:Name="gameFrame" Margin="0,30,0,0"/>
    </Grid>
</Page>
