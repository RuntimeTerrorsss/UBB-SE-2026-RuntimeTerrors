<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OurApp.WinUI.ViewProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OurApp.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Padding="40">
            <StackPanel Padding="20">

                <!-- Company Name -->
                <TextBlock Text="{Binding Company.Name}" 
               FontSize="28" FontWeight="Bold"/>

                <!-- Edit Button -->
                <Button Content="Edit Profile"
            Command="{Binding EditProfileCommand}" />

                <!-- Statistics -->
                <StackPanel Margin="0,20,0,0">

                    <TextBlock Text="Profile Completion"/>

                    <ProgressBar Value="{Binding ProfileCompletion}"
                     Maximum="100"
                     Height="20"/>

                    <TextBlock Text="{Binding ProfileCompletion, StringFormat='{}{0}%'}"/>

                    <ItemsControl ItemsSource="{Binding RemainingTasks}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </StackPanel>

                <!-- Collaborators Preview -->
                <ItemsControl ItemsSource="{Binding Company.Collaborators}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
